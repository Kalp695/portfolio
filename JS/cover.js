/* ----- Couverture Homepage ----- */

var couverture = function(x) {
    var coverTop = window.document.getElementById(x);
    coverTop.style.top = -120 + 'px';
};
var decouvre = function(x) {
    var coverTop = window.document.getElementById(x);
    coverTop.style.top = 0 + 'px';
};